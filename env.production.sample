# Copy to .env.production for local `vite build --mode production`, or set the same
# variable in your host (e.g. Vercel) — never commit real .env files.
#
# Backend (Railway or other public API origin, no trailing slash):
VITE_BACKEND_URL=https://vireact-backend-production-2775.up.railway.app
